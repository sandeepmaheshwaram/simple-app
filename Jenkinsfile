pipeline {
    agent any 
          
          stages {
          stage('build'){
              steps { 
                  sh scrips : 'mvn clean package'
                    }
                }
          }
}
