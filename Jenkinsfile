pipeline {
    agent any 
          
          stages {
          stage('build'){
              steps { 
                  sh script: 'mvn clean package'
                    }
                }
          }
}
